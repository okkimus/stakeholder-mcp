{
  "name": "stakeholder-mcp",
  "version": "1.0.0",
  "description": "MCP server that exposes stakeholder personas for iterative product feedback",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "openai": "^6.18.0",
    "yaml": "^2.8.2",
    "zod": "^4.3.6"
  }
}
