# OpenRouter Configuration (get key at https://openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-v1-your-key-here
DEFAULT_MODEL=anthropic/claude-3-haiku
# Max tokens per consultation response (default: 8192). Lower this to reduce cost; see README Configuration.
# STAKEHOLDER_MCP_MAX_TOKENS=8192

# Optional: For OpenRouter rankings/analytics
APP_URL=http://localhost:3000

# Database
# Path for the SQLite consultation log (default: data/consultations.db)
DB_PATH=data/consultations.db
# Runtime stakeholders are persisted to data/runtime-stakeholders.json (same dir as DB_PATH by default)

# Logging
# Options: debug, info, warn, error
LOG_LEVEL=info
